═══════════════════════════════════════════════════════
    GIT WORKFLOW - AIDE-MÉMOIRE RAPIDE
═══════════════════════════════════════════════════════

📍 COMMENCER UNE FEATURE
──────────────────────────────────────────────────────
git checkout main
git pull origin main
git checkout -b feature/cocktails-nom-feature


💻 TRAVAILLER
──────────────────────────────────────────────────────
# Faire vos modifications...

git add .
git commit -m "feat(cocktails): description"
git push origin feature/cocktails-nom-feature


✅ FINIR LA FEATURE
──────────────────────────────────────────────────────
# Tester que ça fonctionne
pnpm build

# Merger dans main
git checkout main
git merge feature/cocktails-nom-feature
git push origin main

# Supprimer la branche (optionnel)
git branch -d feature/cocktails-nom-feature
git push origin --delete feature/cocktails-nom-feature


🔍 VOIR L'ÉTAT
──────────────────────────────────────────────────────
git status                      # Fichiers modifiés
git log --oneline -10          # Derniers commits
git branch -a                  # Toutes les branches


🔧 ANNULER
──────────────────────────────────────────────────────
git restore .                  # Annuler changements non commités
git reset --soft HEAD~1        # Annuler dernier commit (garder fichiers)


📝 TYPES DE COMMIT
──────────────────────────────────────────────────────
feat       Nouvelle fonctionnalité
fix        Correction de bug
style      CSS, design
refactor   Refactoring
chore      Maintenance
docs       Documentation


🎯 EXEMPLES DE MESSAGES
──────────────────────────────────────────────────────
feat(cocktails): add hero section
fix(cocktails): resolve mobile menu bug
style(cocktails): adjust spacing
chore: update dependencies


═══════════════════════════════════════════════════════

